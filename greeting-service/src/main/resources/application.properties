spring.application.name=greeting-service
server.port=8080

greeting-service.greeting=Ola
greeting-service.default-name=Mundo

spring.profiles.active=es

spring.config.import=optional:configserver:http://localhost:8888/

# Self Registration Pattern
eureka.client.service-url.default-zone=http://localhost:8761/eureka
# Ideal para ambientes containerizados
eureka.instance.prefer-ip-address=true
# Personalizar o id de cada inst\u00E2ncia
eureka.instance.instance-id=${spring.application.name}:${server.port}
# Health Check via Actuator
eureka.client.healthcheck.enabled=true

# Health Check API Pattern
management.endpoints.web.exposure.include=health,info,metrics,circuit-breakers,circuit-breaker-events
management.endpoint.health.show-details=always
management.info.env.enabled=true
management.endpoint.health.probes.enabled=true
management.health.defaults.enabled=true

# Informa\u00E7\u00F5es do /actuator/info
info.app.name=${spring.application.name}
info.app.description=Greeting Microservice - handles greetings in multiple languages
info.app.autor=Pedro Henrique Renosto

# Metadata no Eureka
eureka.instance.metadata-map.github.repo=PedroHenriqueRen11/microservices-resilience
